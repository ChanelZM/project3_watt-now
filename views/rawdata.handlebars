<!-- <main>
    <h2>Raw Data</h2>
    {{# each rawData}}
    <section>
        <h3>{{area}}</h3>
        <img src="{{photo}}" />
    </section>
    {{/ each}}
</main> -->

<main>
    <h2>Raw Data</h2>
    <section>
        <h3>Area 1</h3>
        <figure id="img-container">
            <img src="{{image}}" /> 
        </figure>
    </section>
</main>
